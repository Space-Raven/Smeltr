node_modules/
.next/
dist/
.env
.env.local
.env.production
.turbo/
*.log

# Anchor / Solana
target/
test-ledger/

# Test fixture keypair cache (contains private keys — never commit)
packages/tx-builder/src/__tests__/fixtures/.cache/

# Prisma
apps/web/prisma/migrations/dev/

# Internal corporate / legal / brand documents — local only, never committed.
# (Brand security: keep these referenceable on disk for agents, out of the repo.)
# The Whitepaper is the one public document and stays tracked (negated below).
corporate/
ip-filings/
Smeltr-*.docx
!Smeltr-Whitepaper.docx
# Word lock/temp files
~$*.docx
~$*.doc

# TypeScript incremental build cache (regenerated on every typecheck)
*.tsbuildinfo
